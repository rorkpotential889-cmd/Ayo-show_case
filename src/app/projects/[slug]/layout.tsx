export const metadata = {
  title: 'Project Details',
  description: 'View detailed information about this completed project',
};

export default function ProjectDetailLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
